# Hail-Mary 대시보드 — 디자이너 핸드오프 기능명세서

> 현재 상태: 기능 완결된 프로토타입(순수 HTML/CSS/JS, 프레임워크 없음, 빌드 스텝 없음). 디자이너는 이 문서 기준으로
> 비주얼 디자인을 새로 입히면 됨 — 기능/데이터 흐름은 바뀌지 않는다는 전제. 실제 코드: `index.html`/`styles.css`/`app.js`/`format.js`.

## 1. 개요

- **역할**: M11 통합 대시보드(기능명세서 F-08) — 실시간 점유율, 전력 수요예측(occupancy 반영/미반영 비교), 이상탐지 알림,
  절감/탄소 지표, "마지막 목격" 이미지를 한 화면에서 확인
- **데이터 갱신**: REST 폴링, 5초 간격(WebSocket 없음 — 개발계획서 1.1절 #8 제약)
- **동작 환경**: Hail-mary-Server(FastAPI)가 같은 origin에서 정적 서빙 + API 제공. 브라우저는 상대경로 `fetch("/api/v1/...")`만 씀
- **반응형 브레이크포인트**: 880px(2열→1열), 620px(리포트 3열→2열), 520px(헤더 줄바꿈)
- **다크모드**: 라이트/다크 둘 다 완성. 헤더의 토글 버튼으로 전환(localStorage 저장), 기본은 OS 설정 따라감

## 2. 화면 구성 (위→아래 순서)

### 2.1 헤더
- 좌: 로고 마크(원+점) + "Hail-Mary" + 서브타이틀
- 우: 서버 연결 상태 pill(연결됨/실패, 초록/빨강), 마지막 갱신 시각, 다크모드 토글 버튼

### 2.2 KPI 요약바 (4개 타일, 한 행)
그래프/카드에 들어가기 전 "한눈에 보기" 용. 아래 카드들이 이미 받아온 데이터를 재사용(별도 API 호출 없음).
| 타일 | 값 출처 |
|---|---|
| 현재 총 점유율 | 실시간 점유율 카드와 동일 |
| 열린 이상 알림 | 이상알림 카드 open 건수 |
| occupancy 예측 개선폭 | ablation MAE 개선율 |
| 이번 기간 절감률 | 절감 카드 saved_pct |

데이터 없으면 "–" (0으로 지어내지 않음).

### 2.3 실시간 점유율 카드
- 큰 숫자(전체 인원) + "N개 zone" + zone별 리스트(zone명 : count)
- 빈 상태: "데이터 없음 (아직 수신된 점유율 데이터가 없습니다)"

### 2.4 예측 vs 실제 전력 사용량 (카드 전체폭, 핵심 기능)
- 컨트롤: 건물 선택(select), horizon(시간, number input)
- **그래프/표 전환 토글** — 같은 데이터를 라인차트 또는 표로 볼 수 있음
- 라인 3개: 실제(actual, 점선/중립색) / occupancy 반영 예측(파랑) / occupancy 미반영 예측(주황)
- **호버 시**: 세로 크로스헤어 + 툴팁(그 시점 3개 값 동시 표시)
- x축: 타임스탬프(데이터 많으면 라벨 솎아냄, 최대 6개), y축: 3개 라인 공유 스케일(min/mid/max 3단)
- 범례 + ablation 개선폭 텍스트(예: "occupancy 반영 시 MAE 21.5% / RMSE 23.1% 개선")
- 빈 상태: "데이터 없음 (아직 계산된 예측이 없습니다)"

### 2.5 이상 알림 카드 (open만 표시)
- 리스트 항목: 심각도 뱃지(critical/high/medium/low, 아이콘+색) + zone명 + 시각 + residual_kWh + "확인" 버튼(ack)
- 빈 상태: "열린 이상 알림이 없습니다"

### 2.6 절감 / 탄소 지표 카드
- 기간 라벨 + 4개 지표: 절감 전력량(kWh), 절감률(%), CO2 절감(kg), 나무 환산(그루) — 각 아이콘 포함
- 빈 상태: 지표 그리드 자체를 숨기고 "데이터 없음" 텍스트만

### 2.7 마지막 목격 (카드 전체폭)
- **주의(개인정보)**: zone이 비는(count 0 전환) 순간의 이미지를 **zone당 1장만** 저장·표시. 전환마다 교체(누적 로그/타임라인 아님).
  카메라 원본 영상은 여전히 저장 안 함 — 이 순간 프레임 1장만 예외(제안서.md 9장에 정책 근거 문서화됨)
- 그리드형 썸네일 갤러리: 이미지 + zone명 + 상대시간("5분 전", "3시간 전", 1주 넘으면 절대날짜)
- 빈 상태: "데이터 없음 (아직 zone이 빈 적이 없습니다)"

### 2.8 리포트 (카드 전체폭)
- "인쇄 / PDF 저장" 버튼 → 브라우저 인쇄(이 카드 내용만 인쇄되도록 격리됨, 나머지 화면은 인쇄 시 숨김)
- 내용: 기간, 현재 총 점유율, 절감 전력량/절감률/CO2, occupancy 예측 개선 요약, 열린 이상알림 수, 생성 시각
- 목적: 한 장짜리 요약 리포트를 그 자리에서 뽑아 공유/보관

## 3. API (Hail-mary-Server 제공, 전부 구현·테스트 완료)

| 메서드 | 경로 | 용도 |
|---|---|---|
| GET | `/api/v1/occupancy/live` | zone별 최신 점유 카운트 |
| GET | `/api/v1/forecast?building_id=&horizon=` | 시간대별 with/without occupancy 예측 + 실측 |
| GET | `/api/v1/forecast/ablation?building_id=` | 예측 개선폭(MAE/RMSE) 요약 |
| GET | `/api/v1/anomaly?status=open` | 이상알림 목록 |
| POST | `/api/v1/anomaly/{event_id}/ack` | 알림 확인 처리 |
| GET | `/api/v1/savings` | 절감/탄소 리포트 목록(기간별) |
| GET | `/api/v1/last-seen` | zone별 마지막 목격 메타(zone_id, captured_at, image_url) |
| GET | `/api/v1/last-seen/{zone_id}/image` | 위 이미지 원본(JPEG) |

## 4. 현재 디자인 토큰 (디자이너가 바꿔도 되는 베이스라인)

- **팔레트**: colorblind-safe 검증된 값 사용 중(라이트/다크 둘 다). `styles.css` 상단 `:root` 커스텀 프로퍼티 참고
  - 시리즈: 실제=중립잉크, occupancy반영=blue(`#2a78d6`/`#3987e5`), 미반영=orange(`#eb6834`/`#d95926`)
  - 상태색: good/warning/serious/critical 고정 팔레트(아이콘+라벨 항상 동반 — 색만으로 의미 전달 안 함)
- **타이포**: 시스템 폰트(`system-ui, -apple-system, "Segoe UI", Pretendard, sans-serif`), 별도 웹폰트 없음(데모 네트워크 의존성 최소화 목적)
- **카드**: 흰/짙은 배경 + 얇은 보더 + 은은한 그림자, radius 14px
- **간격 스케일**: 4px 기준 배수(4/8/12/16/24/32)

## 5. 접근성 / 제약사항

- 차트는 색만으로 구분 안 함(범례 + 직접 라벨), 표 보기 대안 항상 존재
- 상태색은 아이콘+텍스트 병행
- 반응형 필수(모바일에서 헤더 줄바꿈·버튼 텍스트 줄바꿈 이슈 이미 한 번 고침 — 디자인 변경 시 재확인 필요)
- 인쇄 스타일(`@media print`)은 리포트 카드 전용 — 다른 카드 디자인 변경 시 이 격리 유지 필요

## 6. 디자이너가 참고할 열린 이슈

- 지금은 기능 검증 우선으로 만든 1차 비주얼 — "그래프 하나만 메인이라 심심하다"는 피드백으로 KPI바/마지막목격/리포트 카드를 추가한 상태. **레이아웃/비주얼 계층은 자유롭게 재설계 가능**
- 로고는 확정본 아님(원+점 placeholder) — 별도 로고 시안 3개가 Claude Design 캔버스로 존재(팀 채널 공유됨), 그중 확정본으로 교체 예정
- 카드 6개+KPI바 배치 순서/그룹핑은 디자이너 판단으로 조정 가능(예: KPI를 카드 안에 흡수, 마지막목격을 이상알림 옆에 배치 등)
